# DEVELOPMENT.md — Nala Developer Guide

> Panduan lengkap untuk kontributor dan maintainer repository monorepo Nala.

---

## 📦 Arsitektur Monorepo

Repositori ini diorganisasi sebagai **pnpm Workspace Monorepo**. Setiap package memiliki tujuan yang sepenuhnya terpisah:

```
nala/                                    ← Root monorepo
├── packages/
│   ├── showcase/                        ← @nala/showcase (Full Demo Application)
│   │   ├── src/
│   │   │   ├── components/              ← 44+ UI Primitives, Layout, CodePreview
│   │   │   ├── views/                   ← 20+ showcase views, charts, RBAC, billing, dll.
│   │   │   ├── stores/                  ← auth, notification, activity, billing, role
│   │   │   └── services/                ← All service adapters including mock services
│   │   └── package.json                 ← name: "@nala/showcase"
│   │
│   └── create-nala/                     ← create-nala (Scaffolding CLI published to npm)
│       ├── src/
│       │   ├── index.ts                 ← CLI entry point (@clack/prompts flow)
│       │   └── utils.ts                 ← File copy, variable replacement, gitignore rename
│       ├── template/                    ← Clean starter template for end users
│       │   └── src/
│       │       ├── components/ui/       ← 34+ UI primitives (subset, no showcase extras)
│       │       ├── layouts/             ← Clean AdminLayout & AuthLayout (no ThemeCustomizer)
│       │       ├── stores/              ← auth.ts ONLY
│       │       ├── services/            ← auth.service.ts + user.service.ts ONLY
│       │       └── views/               ← dashboard, _starter, auth/*, errors/*
│       ├── dist/                        ← Compiled CLI output (generated by tsup)
│       └── package.json                 ← name: "create-nala"
│
├── pnpm-workspace.yaml                  ← Workspace package discovery
├── package.json                         ← Root scripts (delegating to showcase/cli)
├── GEMINI.md / CLAUDE.md               ← AI Guardrails
├── ROADMAP.md                           ← Feature roadmap & milestone tracking
└── DEVELOPMENT.md                       ← This file
```

---

## 🚀 Setup Awal (Initial Setup)

### Prasyarat

- [Node.js](https://nodejs.org/) **v20+**
- [pnpm](https://pnpm.io/) **v9+**  
  ```bash
  npm install -g pnpm
  ```

### Instalasi

```bash
# Clone repositori
git clone https://github.com/candrasp/Nala.git
cd nala

# Install semua workspace dependencies sekaligus
pnpm install
```

> **Mengapa ada `node_modules` di 3 tempat?**  
> Ini adalah perilaku normal pnpm workspace. Folder `node_modules` di setiap package hanya berisi **symlinks** ke folder `.pnpm/` di root — **bukan copy fisik**. Tidak ada penggandaan ukuran disk. Tujuannya adalah **package isolation**: `create-nala` tidak bisa secara tidak sengaja mengakses `vue` atau `reka-ui` yang hanya dideklarasikan di `showcase`.

---

## 💻 Workflow Harian

### Menjalankan Showcase Dev Server

```bash
# Dari root repo
pnpm dev

# Atau langsung dari package showcase
pnpm --filter @nala/showcase dev
```

### Membangun Showcase (Production Build)

```bash
pnpm build
```

### Membangun CLI Tool (create-nala)

```bash
pnpm build:cli

# Atau langsung dari package create-nala
pnpm --filter create-nala build
```

---

## 🔄 Panduan Pengembangan: Kapan Harus Update Apa?

> **Prinsip Emas:** `packages/showcase/` dan `packages/create-nala/template/` berkembang **secara independen**. Tidak ada sinkronisasi otomatis — setiap pembaruan pada template adalah **keputusan yang disengaja**, bukan efek samping.

---

### 🟢 Menambah Fitur Demo Baru di Showcase

*Contoh: Halaman E-Commerce baru, demo Chart, halaman Kanban, mock invoice, dll.*

| Langkah | Lokasi |
|---|---|
| 1. Buat view baru | `packages/showcase/src/views/` |
| 2. Tambahkan route | `packages/showcase/src/router/index.ts` |
| 3. Tambahkan menu | `packages/showcase/src/components/layout/AppSidebar.vue` |
| 4. *(Opsional)* Tambahkan store/service | `packages/showcase/src/stores/` atau `src/services/` |

**Tidak ada tindakan yang diperlukan untuk `create-nala/template/`.**

---

### 🔵 Memperbaiki Bug atau Menambah UI Primitive Core

*Contoh: Bug fix pada `Button.vue`, menambah variant baru pada `Badge.vue`, atau menambah primitive baru `<Tooltip />`.*

| Langkah | Lokasi |
|---|---|
| 1. Perbaiki & uji komponen | `packages/showcase/src/components/ui/` |
| 2. Salin file komponen ke template | `packages/create-nala/template/src/components/ui/` |
| 3. Naikkan versi CLI | `packages/create-nala/package.json` |
| 4. Build & publish CLI | Lihat bagian **Publishing** di bawah |

**Cara cepat sync seluruh folder UI primitives:**

```bash
# Windows (PowerShell) — dari root repo
pnpm sync:ui
```

> Script ini menggunakan `robocopy` (Windows) atau `cp -r` untuk menyalin folder `packages/showcase/src/components/ui/` ke `packages/create-nala/template/src/components/ui/` secara menyeluruh.

#### ✅ Apa yang disalin oleh `sync:ui`:

| Yang Disalin | Lokasi Sumber | Tujuan |
|---|---|---|
| Seluruh folder `components/ui/` | `packages/showcase/src/components/ui/` | `packages/create-nala/template/src/components/ui/` |

#### ❌ Apa yang TIDAK disalin (harus manual):

| Yang TIDAK Disalin | Alasan |
|---|---|
| `components/layout/` (AppNavbar, AppSidebar, dll.) | Template punya versi "cleaned" yang berbeda (tanpa ThemeCustomizer, tanpa NotificationDrawer) |
| `stores/` | Template hanya punya `auth.ts` — showcase punya billing, notification, role, dll. |
| `services/` | Template hanya punya `auth.service.ts` + `user.service.ts` |
| `views/` | Template punya minimal views, showcase punya 20+ views |
| `composables/` | Harus diport manual untuk memastikan tidak ada showcase-specific dependency |

#### ⚠️ Conflict Resolution saat Sync

Jika kamu **menambahkan prop baru** ke sebuah komponen di showcase (misal: `Button.vue` dapat prop `loading`), setelah `sync:ui` komponen template langsung terupdate. **Verifikasi setelah sync:**

```bash
# Jalankan dev server template untuk memastikan tidak ada error
node packages/create-nala/dist/index.js verify-test --force
cd verify-test && pnpm install && pnpm dev
```

Jika ada error TypeScript atau dependency yang hilang di template setelah sync:
1. Cek apakah komponen baru bergantung pada package yang ada di `showcase/package.json` tapi tidak di `template/package.json`.
2. Tambahkan dependency yang dibutuhkan ke `packages/create-nala/template/package.json` secara manual.
3. Hapus output verifikasi: `rm -rf verify-test`

---


### 🟡 Update Layout Utama (AdminLayout, AppNavbar, AppSidebar)

*Layout showcase dan template berbeda — template adalah versi yang sudah "dibersihkan".*

| File di Showcase | File di Template | Perbedaan |
|---|---|---|
| `AdminLayout.vue` | `template/src/layouts/AdminLayout.vue` | Template tidak ada `ThemeCustomizer.vue` |
| `AppNavbar.vue` | `template/src/components/layout/AppNavbar.vue` | Template tidak ada `NotificationDrawer`, tidak ada Paintbrush icon |
| `AppSidebar.vue` | `template/src/components/layout/AppSidebar.vue` | Template hanya `Dashboard` & `Blank Page` |
| `CommandSearchDialog.vue` | `template/src/components/layout/CommandSearchDialog.vue` | Template hanya route starter & error pages |

**Pastikan perubahan diport secara manual ke versi template** dengan mempertahankan perbedaan di atas.

---

### 🟠 Update Auth Store / Composables / Service Layer

*Perubahan pada `auth.ts`, `useThemeConfig.ts`, `auth.service.ts`, `user.service.ts` berdampak pada kedua package.*

| Langkah | Lokasi |
|---|---|
| 1. Update di showcase | `packages/showcase/src/stores/`, `src/composables/`, `src/services/` |
| 2. Update yang sama di template | `packages/create-nala/template/src/stores/`, `src/composables/`, `src/services/` |
| 3. Pastikan `services/index.ts` di template **hanya** export `auth.service` & `user.service` | `packages/create-nala/template/src/services/index.ts` |

---

### 🔴 Upgrade Dependensi Utama (Vue, Vite, Tailwind, Reka UI)

*Kedua package menggunakan versi yang sama secara terpisah — perlu diupdate keduanya.*

```bash
# Update showcase
pnpm --filter @nala/showcase update vue vite tailwindcss reka-ui

# Update template (edit manual packages/create-nala/template/package.json)
# Sesuaikan versi secara manual mengacu pada package showcase
```

> **⚠️ Perhatian:** `packages/create-nala/template/package.json` adalah **file statis** yang akan dikopi ke project pengguna — tidak diinstall dalam workspace. Ubah versinya secara manual.

---

### 🔵 Upgrade Dependensi CLI (clack, kolorist, tsup)

```bash
pnpm --filter create-nala update @clack/prompts kolorist tsup
pnpm build:cli
```

---

## 📋 Tabel Referensi Cepat

| Apa yang Berubah? | Update Showcase? | Update Template? | Rebuild CLI? |
|---|:---:|:---:|:---:|
| Halaman demo / showcase view baru | ✅ | ❌ | ❌ |
| Mock service / data baru | ✅ | ❌ | ❌ |
| Bug fix pada `components/ui/*` | ✅ | ✅ | ✅ |
| Update layout / navbar / sidebar | ✅ | ✅ (dengan perbedaan) | ✅ |
| Update `auth.ts` / `auth.service.ts` | ✅ | ✅ | ✅ |
| Update `useThemeConfig` / composable | ✅ | ✅ | ✅ |
| Upgrade Vue / Vite / Tailwind | ✅ | ✅ (manual) | ✅ |
| Perubahan logika CLI saja | ❌ | ❌ | ✅ |

---

## 🚀 Publishing `create-nala` ke npm

### Persiapan

1. Pastikan `packages/create-nala/template/` sudah up to date dan bersih dari showcase remnants.
2. Naikkan versi di [`packages/create-nala/package.json`](./packages/create-nala/package.json):
   ```json
   {
     "version": "1.0.1"
   }
   ```
3. Build CLI:
   ```bash
   pnpm build:cli
   ```
4. Verifikasi output:
   ```bash
   node packages/create-nala/dist/index.js my-test-project --force
   ```
5. Bersihkan test output:
   ```bash
   powershell -Command "Remove-Item -Recurse -Force my-test-project"
   ```

### Publish

```bash
# Login ke npm (jika belum)
npm login

# Publish package create-nala
pnpm --filter create-nala publish --access public
```

---

## 🧪 Testing

```bash
# Jalankan unit tests (dari root, targeting showcase)
pnpm test

# Atau jalankan sekali (CI mode)
pnpm test:run
```

---

## 🌿 Branching Strategy (Rekomendasi)

| Branch | Tujuan |
|---|---|
| `main` | Kode produksi stabil. Setiap push ke sini memicu CI/CD showcase. |
| `feat/*` | Fitur baru (showcase maupun CLI). |
| `fix/*` | Bug fix. |
| `release/v3.x` | Branch khusus untuk persiapan rilis npm `create-nala`. |

---

## ⚙️ Scripts Tersedia

| Script (dari Root) | Perintah | Keterangan |
|---|---|---|
| `pnpm dev` | `pnpm --filter @nala/showcase dev` | Dev server showcase |
| `pnpm build` | `pnpm --filter @nala/showcase build` | Production build showcase |
| `pnpm preview` | `pnpm --filter @nala/showcase preview` | Preview build showcase |
| `pnpm test` | `pnpm --filter @nala/showcase test` | Unit tests (watch mode) |
| `pnpm test:run` | `pnpm --filter @nala/showcase test:run` | Unit tests (CI mode) |
| `pnpm build:cli` | `pnpm --filter create-nala build` | Compile CLI ke `dist/` |
| `pnpm sync:ui` | PowerShell copy UI primitives | Sync folder `components/ui/` ke template |

---

## 🤖 AI Guardrails

File [`GEMINI.md`](./GEMINI.md) dan [`CLAUDE.md`](./CLAUDE.md) mendefinisikan aturan ketat untuk AI agent yang membantu pengembangan di project ini. Aturan utama yang perlu diketahui pengembang:

1. **Jangan pernah jalankan `pnpm build` secara otomatis** setelah menulis kode.
2. **Jangan install package baru tanpa konfirmasi** eksplisit di chat.
3. **Semua teks UI, komentar kode, dan JSDoc ditulis dalam Bahasa Inggris.**
4. **Selalu gunakan Composition API `<script setup lang="ts">`** — tidak ada Options API.
5. **Tidak ada arbitrary sizing di Tailwind** (`max-w-[300px]` → gunakan skala Tailwind v4).
6. **Tidak ada `any` type** — semua prop, emit, dan payload API harus bertipe eksplisit.
